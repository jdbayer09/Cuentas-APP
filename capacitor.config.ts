import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jdbayer.cuentas.app',
  appName: 'Cuentas',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
